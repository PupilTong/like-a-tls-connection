import { TlsPacketParser } from "../src/TlsPacketParser";
import * as should from 'should'
import * as mocha from 'mocha'
mocha.describe("",()=>{
    const streamData = [
        Buffer.from([]),
        Buffer.from([]),
        Buffer.from([])
    ]
    it("",(done)=>{

    })
})